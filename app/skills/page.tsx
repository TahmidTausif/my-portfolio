import SkillSection from '@/components/SkillSection';
import React from 'react';

const page = () => {
    return (
        <div>
            <SkillSection/>
        </div>
    );
};

export default page;